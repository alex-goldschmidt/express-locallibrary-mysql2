extends layout

block content
  h1= title
  if bookList.length
    ul
      each book in bookList
        li
          a(href=`/catalog/book/${book.bookId}`) #{book.title}
          |  (#{book.author})

  else
    p There are no books.
